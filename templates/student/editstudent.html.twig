{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
<div  class="container" >
{{form_start(form)}}
{{form_widget(form)}}
<input class="btn btn-primary" type="Submit" value="Edit Class"/>
 <a href="{{ app.request.getBaseURL() }}/GetStudent" class="mt-2 btn btn-info" type="Submit">Cancel</a>
{{form_start(form)}}
</div>
{% endblock %}