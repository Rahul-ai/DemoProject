{% extends 'base.html.twig' %}

{% block title %}Add Student  {% endblock %}

{% block body %}
<div  class="container" >
    <h1>Add Student </h1>
    {{form_start(form)}}
    {{ form_widget(form)}}
    <input class="mt-2 btn btn-primary" type="Submit" value="Add Class"/>
</div>
{{form_end(form)}}
{{form_end(form)}}
</div>
</div>

    <div class="mt-3 container">
    <table class="table table-bordered">
    <tr id="a">  
<th id="ClassId">Adm_NO.</th>
<th id="Class_Name">Student_Name</th> 
<th id="Student_Count">Student_Count</th>
<th id="Edit">Edit</th> 
<th id="Delete">Delete</th>  
</tr>
{% set a=1 %}
{% for student in Students %}
 <tr id="a">  
 <td id="{{student.id}}">{{student.admission_number}}</td>   
 <td id="{{student.Name}}">{{student.Name}}</td> 
 <td id="" class="Student_Count">{{student.class_name}}</td> 
 <td><a id="{{student.id}}" href="PutStudent/{{student.id}}" class="edit_data btn btn-info">Edit</a></td>
 <td><button id="{{student.id}}" class="delete_data btn btn-danger">Delete</button></td>
</tr>  
{% set a=a+1 %}
{% endfor %}
   </table>
</div>
{% endblock %}

{% block JQuery %}
<script>
$(document).ready(function(){
    $('.delete_data').click(function(){
        var id = $(this).attr("id");
        if(confirm("Are you sure you want to delete this?"))
        {
         window.location="/DeletedStudent/"+id;
        }
        else
        {
         return false;
        }
    });
});
</script>
{% endblock %}